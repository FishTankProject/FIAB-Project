@model IEnumerable<DSED06_Aquatic_Pet_Store.Models.MARINE_FAMILY>

@{
    ViewBag.Title = "Index";
}

<h2>Marine Family Maintenance</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table table-bordered table-condensed">*@
<table class="table table-bordered table-responsive table-hover">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.TEXT)*@
            Family Name
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TEXT)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_PK }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_PK })
                @*@if(@item.FLAG != true)
                    {

                        @Html.ActionLink("Delete", "Delete", new { id = item.ID_PK })
                    }*@
            </td>
        </tr>
    }

</table>

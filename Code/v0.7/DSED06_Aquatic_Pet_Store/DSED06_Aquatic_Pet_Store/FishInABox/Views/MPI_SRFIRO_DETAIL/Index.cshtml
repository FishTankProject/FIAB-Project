@model IEnumerable<FishInABox.Models.MPI_SRFIRO_DETAIL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<form asp-controller="MPI_SRFIRO_DETAIL" asp-action="Index" method="get">
    <div>
        @Html.Label("Name Search - Leave this empty before searching to search entire table")
    </div>
    <p>
        Search: <input type="text" name="searchString"> <br />
    </p>
    <p>
        <input type="submit" value="Search" /> <br />
    </p>
</form>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SRFIRO_FK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TEXT_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TEXT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SRFIRO_FK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TEXT_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TEXT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PK }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PK })
        </td>
    </tr>
}

</table>
